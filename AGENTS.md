# Leet-Chaser

本地运行 leetcode solution 的命令行框架。使用方法参考 `README.md`

## 开发规范

1. 所有方法必须使用 docstring 注释
2. 用户描述开发需求后，必须首先调研已有代码，判断可行性，并输出可行性报告，提醒用户确认模糊的问题
3. 需求开发阶段规则如下
    - 尽可能复用已有代码
    - 模块化设计和实现：合理拆分类和函数
    - 避免超大函数、大型类
4. 需求开发完成后，必须撰写需求文档
5. 需求文档放在 `docs/` 目录，内容至少包含「需求内容」「设计」「实现方法」三部分；需求内容描述用户目标，设计说明关键取舍，实现方法记录主要模块、接口和验证方式。
6. 完成需求后调用 "收尾进度记录器" 技能进行收尾活动

### PROGRESS 文件更新规则

- 必须在文件末尾写入。除非用户特别强调，否则禁止修改已有条目顺序，禁止在文件其他位置插入
- 保持开发记录为一级列表
- PROGRESS 文件记录每个需求的关键决策点、使用的关键API等信息，禁止流水账式记录例如测试用例数、测试结果等。